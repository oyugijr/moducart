# Example environment variables for local development
# Copy this file to .env and edit any secrets before running docker-compose

# MongoDB used by catalog-api
MONGO_URI=mongodb://mongo:27017/moducart_catalog
CATALOG_PORT=5000

# RabbitMQ used by multiple services
RABBITMQ_URL=amqp://rabbitmq
PRODUCT_STOCK_QUEUE=product.stock

# Ordering API (MySQL)
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=Oyugi222f
MYSQL_DB=ordersdb
ORDERING_PORT=4004

# Discount API (Postgres)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=discountdb
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Basket API (Redis)
REDIS_HOST=redis
REDIS_PORT=6379

# API Gateway
GATEWAY_PORT=3000
SESSION_SECRET=change_me

# Node environment
NODE_ENV=development

# Notes:
# - Do NOT commit real passwords or secrets. Use a secrets manager for production.
# - After copying to .env, run: docker-compose up --build
